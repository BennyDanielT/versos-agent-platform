# Versos triage agent — pinned to the versions verified working in this venv.
# The severity_lab package itself is installed separately, editable:
#   pip install -e nat_sandbox/severity_lab
# (its pyproject pulls nvidia-nat[langchain]; the pins below lock the rest.)

# --- NeMo Agent Toolkit (the orchestration/eval/telemetry layer) ---
nvidia-nat[langchain]==1.8.0
nvidia-nat-eval==1.8.0            # `nat eval` + custom evaluator API
nvidia-nat-opentelemetry==1.8.0  # otelcollector exporter → Phoenix

# --- agent runtime (NAT 1.8 agents need this exact langgraph) ---
langgraph==1.2.6
langchain-nvidia-ai-endpoints==1.4.1

# --- backend (FastAPI embedding the NAT workflow) ---
fastapi==0.137.2
uvicorn==0.49.0
asyncpg==0.30.0                   # async Postgres driver (no SQLAlchemy)
pydantic==2.13.4
pydantic-settings==2.14.1
python-dotenv==1.2.2
truststore==0.10.4               # fixes corporate-proxy SSL (inject_into_ssl)

# --- guardrails (NVIDIA NeMo Guardrails — LLM-powered input/topical rails) ---
nemoguardrails==0.22.0
# PII masking (Presidio) for the customer-facing reply. spaCy model is NOT on PyPI — install via:
#   pip install https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.8.0/en_core_web_sm-3.8.0-py3-none-any.whl
presidio-analyzer==2.2.362
presidio-anonymizer==2.2.362

# --- observability + evals UI (Phoenix) ---
arize-phoenix==17.9.0            # full server: traces + datasets + experiments
arize-phoenix-client==2.9.0      # create_dataset / run_experiment API
arize-phoenix-otel==0.16.1       # phoenix.otel.register (token capture)
openinference-instrumentation-langchain==0.1.66
opentelemetry-exporter-otlp-proto-http==1.42.1
