{
  "permissions": {
    "allow": [
      "PowerShell(python --version)",
      "PowerShell(docker --version)",
      "PowerShell(\"---\")",
      "PowerShell($dd = \"$env:ProgramFiles\\\\Docker\\\\Docker\\\\Docker Desktop.exe\"; if \\(Test-Path $dd\\) { Start-Process $dd; \"starting Docker Desktop...\" } else { \"Docker Desktop.exe not found at $dd\" })"
    ]
  }
}
