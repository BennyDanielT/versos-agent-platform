# EXTRAS — add these ON TOP of requirements-deploy.txt to light up the full experience:
#   * NeMo Guardrails  → the LLM input rail (intent-level jailbreak screening)
#   * Presidio         → PII masking on the customer-facing reply
#   * Phoenix + OTel   → LLM tracing / evals UI (also set PHOENIX_TRACING=1)
#   * nat-eval         → `nat eval` golden-set evaluator
#
# No code change is required — the runtime auto-detects these once installed. In the
# backend Dockerfile, uncomment the extras line and (for Presidio) the spaCy model line.

nvidia-nat-eval==1.8.0
nvidia-nat-opentelemetry==1.8.0

nemoguardrails==0.22.0

presidio-analyzer==2.2.362
presidio-anonymizer==2.2.362
# Presidio also needs the spaCy English model (NOT on PyPI — install by URL):
#   pip install https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.8.0/en_core_web_sm-3.8.0-py3-none-any.whl

arize-phoenix==17.9.0
arize-phoenix-client==2.9.0
arize-phoenix-otel==0.16.1
openinference-instrumentation-langchain==0.1.66
opentelemetry-exporter-otlp-proto-http==1.42.1
